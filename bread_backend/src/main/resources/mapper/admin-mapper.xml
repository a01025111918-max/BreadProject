<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.Bread_project.admin.model.dao.AdminDao">

    <!-- 愿由ъ옄 ?섏씠吏 二쇰Ц ?꾨즺 紐⑸줉 議고쉶 -->
    <select id="selectPaidOrders"
            resultType="adminOrder">
        select
            o.order_no,
            o.member_no,
            m.member_id as memberId,
            nvl(m.member_nickname, m.member_id) as memberNickname,
            o.bread_no,
            b.bread_name as breadName,
            o.order_count,
            o.total_price,
            o.order_status,
            o.order_date
        from order_tbl o
        join member_tbl m on o.member_no = m.member_no
        join bread_tbl b on o.bread_no = b.bread_no
        where o.order_status = 'PAID'
        order by o.order_date desc
    </select>

    <!-- 愿由ъ옄 ?섏씠吏 二쇰Ц 痍⑥냼 紐⑸줉 議고쉶 -->
    <select id="selectCancelOrders"
            resultType="adminOrder">
        select
            o.order_no,
            o.member_no,
            m.member_id as memberId,
            nvl(m.member_nickname, m.member_id) as memberNickname,
            o.bread_no,
            b.bread_name as breadName,
            o.order_count,
            o.total_price,
            o.order_status,
            o.order_date
        from order_tbl o
        join member_tbl m on o.member_no = m.member_no
        join bread_tbl b on o.bread_no = b.bread_no
        where o.order_status in ('CANCEL_REQUEST', 'CANCEL_COMPLETE', 'REFUND_COMPLETE')
        order by o.order_date desc
    </select>

    <!-- 痍⑥냼 ?뱀씤 ?湲?以묒씤 二쇰Ц ?꾩껜 議고쉶 -->
    <select id="selectCancelRequestOrders"
            resultType="adminOrder">
        select
            o.order_no,
            o.member_no,
            m.member_id as memberId,
            nvl(m.member_nickname, m.member_id) as memberNickname,
            o.bread_no,
            b.bread_name as breadName,
            o.order_count,
            o.total_price,
            o.order_status,
            o.order_date
        from order_tbl o
        join member_tbl m on o.member_no = m.member_no
        join bread_tbl b on o.bread_no = b.bread_no
        where o.order_status = 'CANCEL_REQUEST'
        order by o.order_date desc
    </select>

    <!-- 痍⑥냼 ?뱀씤 ?湲?以묒씤 二쇰Ц 1嫄?議고쉶 -->
    <select id="selectCancelRequestOrder"
            parameterType="int"
            resultType="adminOrder">
        select
            o.order_no,
            o.member_no,
            m.member_id as memberId,
            nvl(m.member_nickname, m.member_id) as memberNickname,
            o.bread_no,
            b.bread_name as breadName,
            o.order_count,
            o.total_price,
            o.order_status,
            o.order_date
        from order_tbl o
        join member_tbl m on o.member_no = m.member_no
        join bread_tbl b on o.bread_no = b.bread_no
        where o.order_no = #{orderNo}
          and o.order_status = 'CANCEL_REQUEST'
    </select>

    <!-- 痍⑥냼 ?붿껌 二쇰Ц??痍⑥냼 ?꾨즺 ?곹깭濡?蹂寃?-->
    <update id="completeCancelOrder">
        update order_tbl
        set order_status = 'CANCEL_COMPLETE',
            cancel_completed_at = sysdate,
            cancel_admin_no = #{adminNo}
        where order_no = #{orderNo}
          and order_status = 'CANCEL_REQUEST'
    </update>

    <!-- 痍⑥냼 ?뱀씤??二쇰Ц ?섎웾留뚰겮 鍮??ш퀬 蹂듦뎄 -->
    <update id="restoreBreadStock">
        update bread_tbl
        set bread_stock = bread_stock + #{orderCount}
        where bread_no = #{breadNo}
    </update>

    <!-- 愿由ъ옄 ?곹뭹 ?ш퀬 ?꾩껜 議고쉶 -->
    <select id="selectAdminStocks"
            resultType="adminStock">
        select
            bread_no,
            bread_name,
            bread_price,
            bread_stock,
            bread_category,
            bread_status,
            bread_thumb
        from bread_tbl
        order by bread_no
    </select>

</mapper>
